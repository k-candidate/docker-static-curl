{
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["curl-version.txt"],
      "matchStrings": ["^(?<currentValue>curl-\\d+_\\d+_\\d+)$"],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "curl/curl",
      "versioningTemplate": "regex:^curl-(?<major>\\d+)_(?<minor>\\d+)_(?<patch>\\d+)$"
    }
  ]
}
